"use client";

import { motion } from "framer-motion";

const clientLogos = [
  "Titan",
  "DMRC",
  "Usha",
  "Government of Bihar",
  "State Bank of India",
  "BSNL",
  "IOCL",
  "TVS Motor",
  "L&T",
  "Standard Chartered",
  "HDFC Bank",
  "Reliance",
];

export function ClientLogos() {
  return (
    <section className="py-24 bg-white overflow-hidden">
      <div className="container mx-auto px-6 lg:px-12 mb-12">
        <p className="text-xs uppercase tracking-[0.3em] font-semibold text-neutral-400 mb-6 block">
          Trusted by Industry Leaders
        </p>
      </div>

      <div className="relative flex w-full">
        {/* Infinite Marquee */}
        <motion.div
          className="flex whitespace-nowrap gap-16 md:gap-32 items-center py-4"
          initial={{ x: 0 }}
          animate={{ x: "-50%" }}
          transition={{
            duration: 80,
            repeat: Infinity,
            ease: "linear",
          }}
        >
          {/* Double the logos for seamless loop */}
          {[...clientLogos, ...clientLogos].map((name, index) => (
            <span
              key={`${name}-${index}`}
              className="text-xl md:text-2xl font-serif font-light text-neutral-300 hover:text-neutral-500 transition-colors cursor-default"
            >
              {name}
            </span>
          ))}
        </motion.div>

        {/* Gradient Fades */}
        <div className="absolute inset-y-0 left-0 w-32 bg-linear-to-r from-[#F9F9F9] to-transparent z-10 pointer-events-none" />
        <div className="absolute inset-y-0 right-0 w-32 bg-linear-to-l from-[#F9F9F9] to-transparent z-10 pointer-events-none" />
      </div>
    </section>
  );
}
