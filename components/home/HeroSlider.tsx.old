"use client";

import { Swiper, SwiperSlide } from "swiper/react";
import { Autoplay, EffectFade, Navigation, Pagination } from "swiper/modules";
import { motion, type Variants } from "framer-motion";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import Image from "next/image";

// Import Swiper styles
import "swiper/css";
import "swiper/css/effect-fade";
import "swiper/css/navigation";
import "swiper/css/pagination";

const slides = [
  {
    id: 1,
    image: "/photos/DMRC/dmrc-hero.webp",
    subtitle: "GOVERNMENT PROJECTS",
    title: "Transforming Public Spaces",
    cta: "Explore Products",
    link: "/products",
  },
  {
    id: 2,
    image: "/photos/Usha/usha-hero.webp",
    subtitle: "CORPORATE EXCELLENCE",
    title: "Modern Work Culture",
    cta: "Explore Products",
    link: "/products",
  },
  {
    id: 3,
    image: "/photos/Titan/titan-hero.webp",
    subtitle: "RETAIL & OFFICE",
    title: "Designed for Innovation",
    cta: "Explore Products",
    link: "/products",
  },
  {
    id: 4,
    image: "/photos/Govenment/government-hero.webp",
    subtitle: "GLOBAL STANDARDS",
    title: "World-Class Interiors",
    cta: "Explore Products",
    link: "/products",
  },
];

const container: Variants = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: {
      staggerChildren: 0.15,
    },
  },
};

const item: Variants = {
  hidden: { y: 30, opacity: 0 },
  show: {
    y: 0,
    opacity: 1,
    transition: { duration: 0.8, ease: [0.25, 0.1, 0.25, 1] },
  },
};

export function HeroSlider() {
  return (
    <section className="relative h-[85vh] min-h-[600px] w-full overflow-hidden bg-neutral-900">
      <Swiper
        modules={[Autoplay, EffectFade, Navigation, Pagination]}
        effect="fade"
        speed={1200}
        autoplay={{
          delay: 5000,
          disableOnInteraction: false,
        }}
        pagination={{
          clickable: true,
        }}
        navigation={{
          nextEl: ".swiper-button-next-custom",
          prevEl: ".swiper-button-prev-custom",
        }}
        loop={true}
        className="h-full w-full group"
      >
        {slides.map((slide) => (
          <SwiperSlide key={slide.id} className="relative h-full w-full">
            <div className="absolute inset-0 overflow-hidden">
              <motion.div
                className="absolute inset-0 w-full h-full"
                initial={{ scale: 1 }}
                animate={{ scale: 1.08 }}
                transition={{
                  duration: 12,
                  ease: [0.25, 0.1, 0.25, 1],
                  repeat: Infinity,
                  repeatType: "reverse",
                }}
              >
                <Image
                  src={slide.image}
                  alt={slide.title}
                  fill
                  className="object-cover"
                  priority
                />
              </motion.div>
              <div className="absolute inset-0 bg-black/40" />
            </div>

            <div className="container relative z-10 mx-auto flex h-full items-center px-6 lg:px-12">
              <div className="flex w-full flex-col justify-end pb-24 md:pb-32 md:flex-row md:items-end md:justify-between">
                <motion.div
                  variants={container}
                  initial="hidden"
                  whileInView="show"
                  className="max-w-xl text-white"
                >
                  <motion.div variants={item}>
                    <div className="flex items-center gap-4 mb-4">
                      <div className="w-8 h-[1px] bg-red-600"></div>
                      <p className="text-xs font-semibold tracking-[0.2em] uppercase text-white/90">
                        {slide.subtitle}
                      </p>
                    </div>
                    <h1 className="text-5xl md:text-7xl lg:text-[84px] font-medium tracking-tighter leading-[1.05]">
                      {slide.title}
                    </h1>
                  </motion.div>
                </motion.div>

                <motion.div
                  variants={container}
                  initial="hidden"
                  whileInView="show"
                  className="mt-8 md:mt-0 flex flex-col sm:flex-row gap-4"
                >
                  <motion.div variants={item}>
                    <Link
                      href={slide.link}
                      className="group flex items-center justify-center gap-4 bg-white text-neutral-900 px-8 py-4 transition-all hover:bg-neutral-100 min-w-[200px]"
                    >
                      <span className="text-sm font-semibold uppercase tracking-widest">
                        {slide.cta}
                      </span>
                      <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-1" />
                    </Link>
                  </motion.div>
                  <motion.div variants={item}>
                    <Link
                      href="/contact"
                      className="group flex items-center justify-center gap-2 border border-white/30 hover:border-white px-8 py-4 text-white backdrop-blur-sm transition-all min-w-[200px]"
                    >
                      <span className="text-sm font-semibold uppercase tracking-widest">
                        Contact Us
                      </span>
                    </Link>
                  </motion.div>
                </motion.div>
              </div>
            </div>
          </SwiperSlide>
        ))}

        <div className="swiper-button-prev-custom absolute left-8 top-1/2 z-20 -translate-y-1/2 cursor-pointer text-white/50 hover:text-white transition-colors hidden md:block">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1"
            strokeLinecap="round"
            strokeLinejoin="round"
          >
            <circle
              cx="12"
              cy="12"
              r="11"
              stroke="currentColor"
              fill="none"
            ></circle>
            <path d="M14 8l-4 4 4 4"></path>
          </svg>
        </div>
        <div className="swiper-button-next-custom absolute right-8 top-1/2 z-20 -translate-y-1/2 cursor-pointer text-white/50 hover:text-white transition-colors hidden md:block">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1"
            strokeLinecap="round"
            strokeLinejoin="round"
          >
            <circle
              cx="12"
              cy="12"
              r="11"
              stroke="currentColor"
              fill="none"
            ></circle>
            <path d="M10 8l4 4-4 4"></path>
          </svg>
        </div>
      </Swiper>

      <div className="absolute bottom-8 left-1/2 -translate-x-1/2 z-20 opacity-0 md:opacity-100">
        <div className="w-6 h-10 border border-white/30 rounded-full flex items-start justify-center p-2">
          <div className="w-1 h-2 bg-white/50 rounded-full animate-bounce"></div>
        </div>
      </div>
    </section>
  );
}
