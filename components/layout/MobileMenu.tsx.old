"use client";

import { X, ChevronRight, Search } from "lucide-react";
import Link from "next/link";
import { motion, AnimatePresence } from "framer-motion";
import { useState } from "react";

type MobileMenuProps = {
  isOpen: boolean;
  onClose: () => void;
};

const MAIN_LINKS = [
  { label: "Products", href: "/products" },
  { label: "Solutions", href: "/solutions" },
  { label: "Projects", href: "/gallery" },
  { label: "About Us", href: "/about" },
];

const SECONDARY_LINKS = [
  { label: "3D Configurator", href: "/configurator" },
  { label: "Contact Us", href: "/contact" },
  { label: "Downloads", href: "/downloads" },
  { label: "Our Partners", href: "/about#partners" },
];

export function MobileMenu({ isOpen, onClose }: MobileMenuProps) {
  const [searchQuery, setSearchQuery] = useState("");

  return (
    <AnimatePresence>
      {isOpen && (
        <motion.div
          initial={{ opacity: 0, x: "100%" }}
          animate={{ opacity: 1, x: 0 }}
          exit={{ opacity: 0, x: "100%" }}
          transition={{ type: "tween", duration: 0.4, ease: [0.4, 0, 0.2, 1] }}
          className="fixed inset-0 bg-white z-1100 flex flex-col font-sans overflow-hidden"
        >
          {/* Header */}
          <div className="flex items-center justify-between px-6 h-20 border-b border-neutral-100 shrink-0">
            <Link
              href="/"
              onClick={onClose}
              className="flex items-center gap-3"
            >
              <div className="w-8 h-8 bg-neutral-900 rounded-sm flex items-center justify-center">
                <span className="text-white font-serif text-lg font-light">
                  O
                </span>
              </div>
              <div className="flex flex-col">
                <span className="text-sm font-bold tracking-tight text-neutral-900">
                  One and Only
                </span>
                <span className="text-[8px] uppercase tracking-[0.3em] font-light text-neutral-500 leading-none">
                  Furniture
                </span>
              </div>
            </Link>
            <button
              onClick={onClose}
              title="Close Menu"
              className="w-10 h-10 flex items-center justify-center bg-neutral-50 hover:bg-neutral-100 rounded-full transition-colors"
            >
              <X className="w-5 h-5 text-neutral-900" />
            </button>
          </div>

          {/* Content */}
          <div className="flex-1 overflow-y-auto bg-white px-6">
            {/* Search */}
            <div className="py-8">
              <div className="relative">
                <input
                  type="text"
                  placeholder="Search workspace solutions..."
                  className="w-full h-12 pl-12 pr-4 bg-neutral-50 border border-neutral-100 rounded-none focus:outline-none focus:border-[color:var(--color-primary)] text-base"
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                />
                <Search className="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-neutral-400" />
              </div>
            </div>

            {/* Main Nav */}
            <nav className="mb-12">
              <ul className="space-y-6">
                {MAIN_LINKS.map((link, idx) => (
                  <li key={link.href}>
                    <motion.div
                      initial={{ opacity: 0, x: 20 }}
                      animate={{ opacity: 1, x: 0 }}
                      transition={{ delay: 0.1 + idx * 0.05 }}
                    >
                      <Link
                        href={link.href}
                        onClick={onClose}
                        className="flex items-center justify-between group"
                      >
                        <span className="text-2xl font-light text-neutral-900 group-hover:text-[color:var(--color-primary)] transition-colors tracking-tight">
                          {link.label}
                        </span>
                        <ChevronRight className="w-5 h-5 text-neutral-200 group-hover:text-[color:var(--color-primary)] transition-colors" />
                      </Link>
                    </motion.div>
                  </li>
                ))}
              </ul>
            </nav>

            <div className="h-px bg-neutral-100 mb-10" />

            {/* Secondary Nav */}
            <nav className="mb-12">
              <ul className="grid grid-cols-1 gap-4">
                {SECONDARY_LINKS.map((link, idx) => (
                  <motion.li
                    key={link.href}
                    initial={{ opacity: 0 }}
                    animate={{ opacity: 1 }}
                    transition={{ delay: 0.3 + idx * 0.05 }}
                  >
                    <Link
                      href={link.href}
                      onClick={onClose}
                      className="text-base text-neutral-500 hover:text-neutral-900 transition-colors"
                    >
                      {link.label}
                    </Link>
                  </motion.li>
                ))}
              </ul>
            </nav>
          </div>

          {/* Footer */}
          <div className="bg-neutral-50 p-6 border-t border-neutral-100 shrink-0">
            <div className="space-y-4">
              <p className="text-[10px] uppercase tracking-widest text-neutral-400 font-bold">
                Authorized Strategic Partner of AFC India
              </p>
              <div className="flex items-center justify-between">
                <div className="flex gap-4 items-center">
                  <button className="text-[10px] font-bold border border-neutral-300 rounded-full w-6 h-6 flex items-center justify-center">
                    EN
                  </button>
                  <span className="text-xs text-neutral-400">Patna, India</span>
                </div>
                <div className="text-[10px] text-neutral-400 font-medium">
                  © {new Date().getFullYear()} One and Only Furniture
                </div>
              </div>
            </div>
          </div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
